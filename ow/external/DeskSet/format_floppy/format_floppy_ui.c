/*
 * format_floppy_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `format_floppy.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include <group.h>
#include "format_floppy_ui.h"

/*
 * Initialize an instance of object `base_frame'.
 */
format_floppy_base_frame_objects *
format_floppy_base_frame_objects_initialize(format_floppy_base_frame_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (format_floppy_base_frame_objects *) calloc(1, sizeof (format_floppy_base_frame_objects))))
		return (format_floppy_base_frame_objects *) NULL;
	if (!ip->base_frame)
		ip->base_frame = format_floppy_base_frame_base_frame_create(ip, owner);
	return ip;
}

/*
 * Create object `base_frame' in the specified instance.
 */
Xv_opaque
format_floppy_base_frame_base_frame_create(format_floppy_base_frame_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 400,
		XV_HEIGHT, 150,
		XV_LABEL, dgettext("SUNW_DESKSET_DS_FORMAT", "Format Program"),
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		NULL);
	return obj;
}

/*
 * Initialize an instance of object `format_popup'.
 */
format_floppy_format_popup_objects *
format_floppy_format_popup_objects_initialize(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (format_floppy_format_popup_objects *) calloc(1, sizeof (format_floppy_format_popup_objects))))
		return (format_floppy_format_popup_objects *) NULL;
	if (!ip->format_popup)
		ip->format_popup = format_floppy_format_popup_format_popup_create(ip, owner);
	if (!ip->format_panel)
		ip->format_panel = format_floppy_format_popup_format_panel_create(ip, ip->format_popup);
	if (!ip->format_msg_1)
		ip->format_msg_1 = format_floppy_format_popup_format_msg_1_create(ip, ip->format_panel);
	if (!ip->format_msg_2)
		ip->format_msg_2 = format_floppy_format_popup_format_msg_2_create(ip, ip->format_panel);
	if (!ip->format_msg_3)
		ip->format_msg_3 = format_floppy_format_popup_format_msg_3_create(ip, ip->format_panel);
	if (!ip->format_choice)
		ip->format_choice = format_floppy_format_popup_format_choice_create(ip, ip->format_panel);
	if (!ip->format_diskname)
		ip->format_diskname = format_floppy_format_popup_format_diskname_create(ip, ip->format_panel);
	if (!ip->format_format)
		ip->format_format = format_floppy_format_popup_format_format_create(ip, ip->format_panel);
	if (!ip->group4)
		ip->group4 = format_floppy_format_popup_group4_create(ip, ip->format_panel);
	if (!ip->format_cancel)
		ip->format_cancel = format_floppy_format_popup_format_cancel_create(ip, ip->format_panel);
	window_fit(ip->format_panel);
	
	window_fit(ip->format_popup);
	
	xv_set(ip->format_panel,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		NULL);
	return ip;
}

/*
 * Create object `format_popup' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_popup_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 534,
		XV_HEIGHT, 343,
		XV_LABEL, dgettext("SUNW_DESKSET_DS_FORMAT", "Format Floppy"),
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `format_panel' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_panel_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_panel",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `format_msg_1' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_msg_1_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 130,
		XV_Y, 50,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Are you sure you want to format this floppy disk?"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `format_msg_2' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_msg_2_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 50,
		XV_Y, 80,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "WARNING:   Formatting will destroy any information on the disk"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `format_msg_3' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_msg_3_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 130,
		XV_Y, 100,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "and is not undoable."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `format_choice' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_choice_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_CHOICE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_choice",
		XV_X, 130,
		XV_Y, 142,
		PANEL_CHOICE_NROWS, 3,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_CHOOSE_NONE, FALSE,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Please select a format:"),
		PANEL_CHOICE_STRINGS,
			dgettext("SUNW_DESKSET_DS_FORMAT", "UNIX"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "DOS High Density"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "NEC-DOS Medium Density"),
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_diskname' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_diskname_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	store_label(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_diskname",
		XV_X, 130,
		XV_Y, 250,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 80,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Disk Name:"),
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, store_label,
		NULL);
	return obj;
}

/*
 * Create object `format_format' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_format_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	extern void		format(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_format",
		XV_X, 130,
		XV_Y, 280,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Format Disk"),
		PANEL_NOTIFY_PROC, format,
		NULL);
	return obj;
}

/*
 * Create object `group4' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_group4_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 50,
		XV_Y, 50,
		GROUP_TYPE, GROUP_NONE,
		GROUP_MEMBERS,
			ip->format_msg_1,
			ip->format_msg_2,
			ip->format_msg_3,
			ip->format_choice,
			ip->format_diskname,
			ip->format_format,
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_cancel' in the specified instance.
 */
Xv_opaque
format_floppy_format_popup_format_cancel_create(format_floppy_format_popup_objects *ip, Xv_opaque owner)
{
	extern void		cancel(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_cancel",
		XV_X, 360,
		XV_Y, 280,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Cancel"),
		PANEL_NOTIFY_PROC, cancel,
		NULL);
	return obj;
}

/*
 * Initialize an instance of object `unlabeled_popup'.
 */
format_floppy_unlabeled_popup_objects *
format_floppy_unlabeled_popup_objects_initialize(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (format_floppy_unlabeled_popup_objects *) calloc(1, sizeof (format_floppy_unlabeled_popup_objects))))
		return (format_floppy_unlabeled_popup_objects *) NULL;
	if (!ip->unlabeled_popup)
		ip->unlabeled_popup = format_floppy_unlabeled_popup_unlabeled_popup_create(ip, owner);
	if (!ip->unlabeled_panel)
		ip->unlabeled_panel = format_floppy_unlabeled_popup_unlabeled_panel_create(ip, ip->unlabeled_popup);
	if (!ip->unlabeled_msg_1)
		ip->unlabeled_msg_1 = format_floppy_unlabeled_popup_unlabeled_msg_1_create(ip, ip->unlabeled_panel);
	if (!ip->unlabeled_msg_2)
		ip->unlabeled_msg_2 = format_floppy_unlabeled_popup_unlabeled_msg_2_create(ip, ip->unlabeled_panel);
	if (!ip->unlabeled_msg_3)
		ip->unlabeled_msg_3 = format_floppy_unlabeled_popup_unlabeled_msg_3_create(ip, ip->unlabeled_panel);
	if (!ip->unlabeled_msg_4)
		ip->unlabeled_msg_4 = format_floppy_unlabeled_popup_unlabeled_msg_4_create(ip, ip->unlabeled_panel);
	if (!ip->unlabeled_msg_5)
		ip->unlabeled_msg_5 = format_floppy_unlabeled_popup_unlabeled_msg_5_create(ip, ip->unlabeled_panel);
	if (!ip->format_choice)
		ip->format_choice = format_floppy_unlabeled_popup_format_choice_create(ip, ip->unlabeled_panel);
	if (!ip->format_diskname)
		ip->format_diskname = format_floppy_unlabeled_popup_format_diskname_create(ip, ip->unlabeled_panel);
	if (!ip->format_format)
		ip->format_format = format_floppy_unlabeled_popup_format_format_create(ip, ip->unlabeled_panel);
	if (!ip->group2)
		ip->group2 = format_floppy_unlabeled_popup_group2_create(ip, ip->unlabeled_panel);
	if (!ip->format_cancel)
		ip->format_cancel = format_floppy_unlabeled_popup_format_cancel_create(ip, ip->unlabeled_panel);
	if (!ip->format_eject)
		ip->format_eject = format_floppy_unlabeled_popup_format_eject_create(ip, ip->unlabeled_panel);
	window_fit(ip->unlabeled_panel);
	
	window_fit(ip->unlabeled_popup);
	
	xv_set(ip->unlabeled_panel,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		NULL);
	return ip;
}

/*
 * Create object `unlabeled_popup' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_popup_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 546,
		XV_HEIGHT, 363,
		XV_LABEL, dgettext("SUNW_DESKSET_DS_FORMAT", "Unlabeled Floppy"),
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `unlabeled_panel' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_panel_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:unlabeled_panel",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `unlabeled_msg_1' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_msg_1_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 40,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "This floppy disk is unreadable by the system."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unlabeled_msg_2' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_msg_2_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 60,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "It may contain tar or bar formatted data."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unlabeled_msg_3' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_msg_3_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 100,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Do you want to format it?"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unlabeled_msg_4' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_msg_4_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 60,
		XV_Y, 140,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "WARNING:   Formatting will destroy any information on the disk"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unlabeled_msg_5' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_unlabeled_msg_5_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 160,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "and is not undoable."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `format_choice' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_format_choice_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_CHOICE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_choice",
		XV_X, 140,
		XV_Y, 200,
		PANEL_CHOICE_NROWS, 3,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_CHOOSE_NONE, FALSE,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Please select a format:"),
		PANEL_CHOICE_STRINGS,
			dgettext("SUNW_DESKSET_DS_FORMAT", "UNIX"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "DOS High Density"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "NEC-DOS Medium Density"),
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_diskname' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_format_diskname_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	store_label(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_diskname",
		XV_X, 140,
		XV_Y, 300,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 80,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Disk Name:"),
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, store_label,
		NULL);
	return obj;
}

/*
 * Create object `format_format' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_format_format_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	extern void		unlabeled_format(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_format",
		XV_X, 80,
		XV_Y, 340,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Format Disk"),
		PANEL_NOTIFY_PROC, unlabeled_format,
		NULL);
	return obj;
}

/*
 * Create object `group2' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_group2_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 60,
		XV_Y, 40,
		GROUP_TYPE, GROUP_NONE,
		GROUP_MEMBERS,
			ip->unlabeled_msg_1,
			ip->unlabeled_msg_2,
			ip->unlabeled_msg_3,
			ip->unlabeled_msg_4,
			ip->unlabeled_msg_5,
			ip->format_choice,
			ip->format_diskname,
			ip->format_format,
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_cancel' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_format_cancel_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	extern void		cancel(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_cancel",
		XV_X, 240,
		XV_Y, 340,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Cancel"),
		PANEL_NOTIFY_PROC, cancel,
		NULL);
	return obj;
}

/*
 * Create object `format_eject' in the specified instance.
 */
Xv_opaque
format_floppy_unlabeled_popup_format_eject_create(format_floppy_unlabeled_popup_objects *ip, Xv_opaque owner)
{
	extern void		eject(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_eject",
		XV_X, 410,
		XV_Y, 340,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Cancel & Eject"),
		PANEL_NOTIFY_PROC, eject,
		NULL);
	return obj;
}

/*
 * Initialize an instance of object `unformatted_popup'.
 */
format_floppy_unformatted_popup_objects *
format_floppy_unformatted_popup_objects_initialize(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (format_floppy_unformatted_popup_objects *) calloc(1, sizeof (format_floppy_unformatted_popup_objects))))
		return (format_floppy_unformatted_popup_objects *) NULL;
	if (!ip->unformatted_popup)
		ip->unformatted_popup = format_floppy_unformatted_popup_unformatted_popup_create(ip, owner);
	if (!ip->unformatted_panel)
		ip->unformatted_panel = format_floppy_unformatted_popup_unformatted_panel_create(ip, ip->unformatted_popup);
	if (!ip->unformatted_msg_1)
		ip->unformatted_msg_1 = format_floppy_unformatted_popup_unformatted_msg_1_create(ip, ip->unformatted_panel);
	if (!ip->unformatted_msg_2)
		ip->unformatted_msg_2 = format_floppy_unformatted_popup_unformatted_msg_2_create(ip, ip->unformatted_panel);
	if (!ip->unformatted_group_2)
		ip->unformatted_group_2 = format_floppy_unformatted_popup_unformatted_group_2_create(ip, ip->unformatted_panel);
	if (!ip->unformatted_msg_3)
		ip->unformatted_msg_3 = format_floppy_unformatted_popup_unformatted_msg_3_create(ip, ip->unformatted_panel);
	if (!ip->unformatted_msg_4)
		ip->unformatted_msg_4 = format_floppy_unformatted_popup_unformatted_msg_4_create(ip, ip->unformatted_panel);
	if (!ip->unformatted_msg_5)
		ip->unformatted_msg_5 = format_floppy_unformatted_popup_unformatted_msg_5_create(ip, ip->unformatted_panel);
	if (!ip->format_choice)
		ip->format_choice = format_floppy_unformatted_popup_format_choice_create(ip, ip->unformatted_panel);
	if (!ip->format_diskname)
		ip->format_diskname = format_floppy_unformatted_popup_format_diskname_create(ip, ip->unformatted_panel);
	if (!ip->format_format)
		ip->format_format = format_floppy_unformatted_popup_format_format_create(ip, ip->unformatted_panel);
	if (!ip->group3)
		ip->group3 = format_floppy_unformatted_popup_group3_create(ip, ip->unformatted_panel);
	if (!ip->format_cancel)
		ip->format_cancel = format_floppy_unformatted_popup_format_cancel_create(ip, ip->unformatted_panel);
	if (!ip->format_eject)
		ip->format_eject = format_floppy_unformatted_popup_format_eject_create(ip, ip->unformatted_panel);
	window_fit(ip->unformatted_panel);
	
	window_fit(ip->unformatted_popup);
	
	xv_set(ip->unformatted_panel,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		NULL);
	return ip;
}

/*
 * Create object `unformatted_popup' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_popup_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 546,
		XV_HEIGHT, 402,
		XV_LABEL, dgettext("SUNW_DESKSET_DS_FORMAT", "Unformatted Floppy"),
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `unformatted_panel' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_panel_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:unformatted_panel",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_msg_1' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_msg_1_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 40,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "This floppy disk is unreadable.  It may be blank"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_msg_2' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_msg_2_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 61,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "or contain data in an unreadable format."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_group_2' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_group_2_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 40,
		GROUP_TYPE, GROUP_COLUMN,
		GROUP_MEMBERS,
			ip->unformatted_msg_1,
			ip->unformatted_msg_2,
			NULL,
		GROUP_COLUMN_ALIGNMENT, GROUP_LEFT_EDGES,
		GROUP_VERTICAL_SPACING, 5,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_msg_3' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_msg_3_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 90,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Do you want to format it?"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_msg_4' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_msg_4_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 60,
		XV_Y, 120,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "WARNING:   Formatting will destroy any information on the disk"),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `unformatted_msg_5' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_unformatted_msg_5_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_MESSAGE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 140,
		XV_Y, 140,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "and is not undoable."),
		PANEL_LABEL_BOLD, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `format_choice' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_format_choice_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_CHOICE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_choice",
		XV_X, 140,
		XV_Y, 190,
		PANEL_CHOICE_NROWS, 3,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_CHOOSE_NONE, FALSE,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Please select a format:"),
		PANEL_CHOICE_STRINGS,
			dgettext("SUNW_DESKSET_DS_FORMAT", "UNIX"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "DOS High Density"),
			dgettext("SUNW_DESKSET_DS_FORMAT", "NEC-DOS Medium Density"),
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_diskname' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_format_diskname_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	store_label(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_diskname",
		XV_X, 140,
		XV_Y, 290,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 80,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Disk Name:"),
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, store_label,
		NULL);
	return obj;
}

/*
 * Create object `format_format' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_format_format_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	extern void		unformatted_format(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_format",
		XV_X, 140,
		XV_Y, 330,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Format Disk"),
		PANEL_NOTIFY_PROC, unformatted_format,
		NULL);
	return obj;
}

/*
 * Create object `group3' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_group3_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 60,
		XV_Y, 40,
		GROUP_TYPE, GROUP_NONE,
		GROUP_MEMBERS,
			ip->unformatted_group_2,
			ip->unformatted_msg_3,
			ip->unformatted_msg_4,
			ip->unformatted_msg_5,
			ip->format_choice,
			ip->format_diskname,
			ip->format_format,
			NULL,
		NULL);
	return obj;
}

/*
 * Create object `format_cancel' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_format_cancel_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	extern void		cancel(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_cancel",
		XV_X, 260,
		XV_Y, 330,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Cancel"),
		PANEL_NOTIFY_PROC, cancel,
		NULL);
	return obj;
}

/*
 * Create object `format_eject' in the specified instance.
 */
Xv_opaque
format_floppy_unformatted_popup_format_eject_create(format_floppy_unformatted_popup_objects *ip, Xv_opaque owner)
{
	extern void		eject(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "format_floppy:format_eject",
		XV_X, 400,
		XV_Y, 330,
		PANEL_LABEL_STRING, dgettext("SUNW_DESKSET_DS_FORMAT", "Cancel & Eject"),
		PANEL_NOTIFY_PROC, eject,
		NULL);
	return obj;
}

